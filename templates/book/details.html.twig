{% extends 'base.html.twig' %}

{% block title %}Book Details{% endblock %}

{% block body %}
<div class="container mt-5">
    <h1 class="text-center mb-4">📘 Book Details</h1>

    {% if book %}
        <div class="card shadow p-4 mb-4">
            <h3 class="card-title mb-3 text-primary">{{ book.title }}</h3>

            <table class="table table-bordered">
                <tbody>
                    <tr>
                        <th>Author</th>
                        <td>
                            {% if book.author %}
                                {{ book.author.name ?? 'Unknown' }}
                            {% else %}
                                <em>Unknown</em>
                            {% endif %}
                        </td>
                    </tr>
                    <tr>
                        <th>Category</th>
                        <td>{{ book.category }}</td>
                    </tr>
                    <tr>
                        <th>Publication Date</th>
                        <td>{{ book.publicationDate ? book.publicationDate|date('Y-m-d') : 'N/A' }}</td>
                    </tr>
                    <tr>
                        <th>Published</th>
                        <td>{{ book.published ? 'Yes' : 'No' }}</td>
                    </tr>
                    <tr>
                        <th>Reference (ID)</th>
                        <td>{{ book.ref }}</td>
                    </tr>
                </tbody>
            </table>
        </div>

        <div class="d-flex justify-content-between">
            <a href="{{ path('app_book_list') }}" class="btn btn-secondary">← Back to List</a>

            <div>
                <a href="{{ path('app_book_edit', {'ref': book.ref}) }}" class="btn btn-warning me-2">✏️ Edit</a>

                <form action="{{ path('app_book_delete', {'ref': book.ref}) }}" method="post" style="display:inline"
                      onsubmit="return confirm('Are you sure you want to delete this book?');">
                    <input type="hidden" name="_token" value="{{ csrf_token('delete-book-' ~ book.ref) }}">
                    <button type="submit" class="btn btn-danger">🗑 Delete</button>
                </form>
            </div>
        </div>
    {% else %}
        <div class="alert alert-danger text-center">❌ Book not found.</div>
    {% endif %}
</div>
{% endblock %}
